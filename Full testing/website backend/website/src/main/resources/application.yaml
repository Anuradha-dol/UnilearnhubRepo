spring:
  application:
    name: website

  servlet:
    multipart:
      enabled: true
      max-file-size: 250MB
      max-request-size: 500MB

  datasource:
    url: jdbc:postgresql://localhost:5432/itpm_uni_learnhub_project
    username: postgres
    password: laksara
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 10000
      leak-detection-threshold: 20000

  jpa:
    hibernate:
      ddl-auto: update   # dev only, prod -> validate

  mail:
    host: smtp.gmail.com
    port: 587
    username: "anuradhawork123@gmail.com"
    password: "umae tfzt zkli wjow"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jwt:
    secret: "Y2hhbGxlbmdlVG9pcmu0ZU41bGxQcmVkaWN0YWJsZVNlY3JldEtleQ=="

  cookie:
    secure: false     # true in production (HTTPS)
    same-site: Lax    # None if frontend & backend are on different domains

server:
  port: 8081
